## A toy redis server, written in Golang.

It supports few the features of Redis
- reachable over TCP.
- can handle concurrent requests.
- GET, SET, DEL, INCR methods are implemented.
- uses only standard libraries.